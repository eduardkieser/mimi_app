version: '3.8'

services:
  mimi-app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: mimi-today
    restart: unless-stopped
    ports:
      - "8001:8001"
    volumes:
      # Persist SQLite database
      - ./backend/data:/app/data
      # Persist logs
      - ./backend/logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

